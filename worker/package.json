{
  "name": "commons-registration-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Upload backend for the PAWN × Commons registration form (Cloudflare Worker + R2 + Resend)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
